<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.WeatherAppPositivo" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <!-- Para que los iconos sean oscuros en fondo claro -->
        <item name="android:windowLightStatusBar">true</item>

        <!-- Color de la barra de estado -->
        <item name="android:statusBarColor">@color/white</item>
    </style>

    <style name="Theme.WeatherAppPositivo" parent="Base.Theme.WeatherAppPositivo" />
</resources>